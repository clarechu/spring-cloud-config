spring:
  application:
    name: foo
  profiles:
    active:
      - dev
    include:
      - db
  cloud:
    config:
      uri: http://localhost:${config.port:8888}
      watch:
        enabled: true
